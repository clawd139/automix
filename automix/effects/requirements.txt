# Core dependencies
torch>=2.0.0
torchaudio>=2.0.0

# Optional: For audio file I/O in tests
# torchaudio handles most formats, but you may need:
# soundfile>=0.12.0  # For some audio formats

# Optional: For stem separation (run separately)
# demucs>=4.0.0

# Development/Testing
pytest>=7.0.0
numpy>=1.21.0
